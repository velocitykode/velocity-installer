package routes

import (
	"{{.Module}}/app/http/controllers"

	"github.com/velocitykode/velocity/pkg/router"
)

func init() {
	router.Register(func(r router.Router) {
		r.Get("/", controllers.Home).Name("home")
	})
}
